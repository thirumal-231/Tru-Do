<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-slate-700 h-screen flex justify-center items-center">
    <div class="w-2/4 bg-slate-800 p-6 rounded-lg shadow-lg">
      <!-- Header -->
      <h1 class="text-center text-white text-4xl font-bold mb-6">Edit To-Do</h1>

      <!-- Edit Form -->
      <form
        method="post"
        action="/todos/<%= todoToEdit._id %>?_method=PATCH"
        class="space-y-6"
      >
        <!-- Title Input -->
        <div class="flex flex-col space-y-2">
          <label for="title" class="text-white text-lg font-semibold">
            Title
          </label>
          <textarea
            name="title"
            id="title"
            class="rounded-lg px-4 py-3 text-xl bg-gray-100 shadow focus:outline-none focus:ring-2 focus:ring-blue-400 resize-none"
            rows="2"
          >
<%= todoToEdit.title %></textarea
          >
        </div>

        <!-- Description Input -->
        <div class="flex flex-col space-y-2">
          <label for="description" class="text-white text-lg font-semibold">
            Description
          </label>
          <textarea
            name="description"
            id="description"
            class="rounded-lg px-4 py-3 text-xl bg-gray-100 shadow focus:outline-none focus:ring-2 focus:ring-blue-400 resize-none"
            rows="4"
          >
<%= todoToEdit.description %></textarea
          >
        </div>

        <!-- Buttons -->
        <div
          class="flex flex-wrap justify-between space-x-4 space-y-4 sm:space-y-0"
        >
          <!-- Back Button -->
          <a
            href="/todos"
            class="px-6 py-3 bg-orange-500 text-white rounded-lg shadow hover:bg-orange-600 transition duration-300 text-center w-full sm:w-auto"
          >
            BACK
          </a>

          <!-- Save Button -->
          <button
            type="submit"
            class="px-6 py-3 bg-green-500 text-white rounded-lg shadow hover:bg-green-600 transition duration-300 w-full sm:w-auto"
          >
            SAVE
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
